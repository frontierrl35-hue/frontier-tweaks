import { CategoryPage } from '../components/layout/CategoryPage';

export default function GeneralPage() {
  return (
    <CategoryPage
      category="general"
      title="General"
      subtitle="Everyday Windows behavior — notifications, animations, search, and startup."
    />
  );
}
